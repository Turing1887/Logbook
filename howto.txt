How to install npm modules:

Just run npm install in your console after you've installed -->node.js<--. 
It will install all the npm modules mentioned in the package.json automatically.

How to run:

Open your console and go into the folder Logbook is in(cd/"foldername").Type in "npm start" in your console.
